[package]
name = "er-sidekick"
version = "0.1.0"
edition = "2021"

[dependencies]
md5 = "0.7.0"
once_cell = "1.19"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
opt-level = 3
lto = true
